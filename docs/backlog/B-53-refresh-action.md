---
id: B-53
title: "refresh: перезагрузить текущий экран, не называя его"
status: done
priority: P2
size: S
stage: release-0.38
---

# B-53 — refresh: перезагрузить текущий экран, не называя его

Самый частый ответ на `perform` — «покажи тот же экран заново». Сегодня это `navigate` на
собственный deeplink (§4.4), и у этого две цены: сервер обязан знать, с какого экрана пришло
действие (а для строки в общем списке это не всегда он), и клиент кладёт в стек навигации второй
экземпляр того же экрана, если приложение не догадалось сравнить.

- **Решение: действие `refresh`** без полей. Клиент перезапрашивает эндпоинт текущего экрана и
  применяет новое дерево по правилам §4.4 (состояние узлов под стабильными `id` переживает).
- Старый клиент: `UNKNOWN_ACTION`, экран не обновится. На переходный период сервер может отвечать
  `navigate` клиентам, объявившим старую версию, — записать в §15 как пример.

- AC: `perform` отвечает `refresh` → экран перезапрошен, стек навигации не вырос, позиция списка
  сохранена; SPEC §16.4.
- Якоря: `kompot-standard/.../Components.kt`, `kompot-navigation`, `kompot-spec/SPEC.md` §4.4, §16.4.

## Находки

### Итерация 1 — 2026-09-25

**Загрузку экрана ведёт приложение, не тулкит** — как и для `navigate`. Поэтому у тулкита три части:
тип `refresh`, обёртка `withRefresh(scope, reload)` рядом с `withPerform` и гарантия, от которой
`refresh` зависит: новое дерево под теми же `id` сохраняет позицию списка (§4.4).

**AC «стек не вырос, позиция сохранена» — позиция проверена тестом, стек нет.** Стек навигации
принадлежит приложению; `refresh` не переход по определению, и тулкиту в нём расти нечему. Позиция —
тест `RefreshTest`: прокрутка к строке 30, `refresh`, на экране строка 30 с новым текстом, а не
начало. Мутация «новое состояние списка на каждое дерево» убита им — то есть тест меряет именно
позицию, а не что-то проходящее и так.

**Мутации:** `reload` не вызывается — убита; новое `LazyListState` на каждое дерево — убита.
Полная сборка на Linux-машине — со второй попытки (снова «Not enough memory» на production-wasm).

