---
id: B-58
title: "Выпуск 0.38.0: версия, журналы, Central"
status: wip
priority: P0
size: S
stage: release-0.38
blocked_by: [B-40, B-41, B-42, B-44, B-45, B-46, B-47, B-48, B-49, B-50, B-51, B-52, B-53, B-54, B-55, B-56, B-57, B-62, B-63]
---

# B-58 — Выпуск 0.38.0: версия, журналы, Central

Замыкающая задача этапа `release-0.38`. Последний выпуск на Central — 0.37.0. `version` в
`gradle.properties` уже поднят до 0.38.0 тем же изменением, что завело этот этап, и непрерывный канал
публикует `0.38.0.<run>`.

- **0.38.0, а не 1.0.** `UPGRADING.md` прямо говорит, что `0.x` ничего не обещает, кроме записи о
  поломке. 1.0 — обещание стабильности API и провода, и это отдельное решение, а не следствие объёма
  релиза. Если принимать его — отдельной задачей с тем, что именно обещается.
- **Почему версия поднята в начале, а не в конце.** Иначе всё, что вливается по пути, уезжало бы
  в непрерывный канал как `0.37.0.<run>` — сборкой, неотличимой по номеру от выпущенной 0.37.0, но
  несущей Compose 1.12 и новые слова провода.
- Журналы:
  - `UPGRADING.md` — Kotlin-поломки: Compose 1.12 у потребителя ([B-40](B-40-compose-line-1-12.md)),
    всё, что покажет `checkKotlinAbi`;
  - §13 SPEC — несовместимые изменения провода, если хоть одно из новых слов шло не по §15;
  - README модулей — новые типы и действия (сниппеты проверяются, [B-37](B-37-readme-snippets-are-unchecked.md)).
- **viddik — снова релиз.** С исправлением youndie/viddik#44 kompot взял snapshot `0.6.0.40` из
  Reposilite (только тесты, в опубликованное не попадает). До выпуска — следующий релиз viddik с
  Central, чтобы сборка 0.38.0 не зависела от нашего snapshot-репозитория.
- Публикация — диспатч `central.yaml` в sborka; бандл читается глазами до Publish (Central не
  переписывает версию).
- Задачи, которые к релизу не успели, снимаются с `blocked_by` явно, с причиной в этом файле, а не
  молча.
  - **B-43 снята 2026-09-25:** #51 не воспроизводится на 1.11 и 1.12, владелец закрыл issue как
    невоспроизводимый ([B-43](B-43-multiline-field-eats-the-wheel.md), «Решение»).
  - **B-63 добавлена 2026-09-25:** проверка совместимости не видит изменений базы иерархии, а этот
    релиз её меняет (`fallback` в базе `KompotComponent`, B-49) — выпускать по проверке, которая не
    смотрит на изменённое место, нельзя.
  - **B-62 добавлена 2026-09-25:** выделена из B-54 в стадии этого релиза; решение — соглашение
    об `id`, записанное в SPEC, и оно должно попасть в тот же выпуск, что и показ по видимости.

- AC: `io.github.youndie:kompot-bom:0.38.0` резолвится с Central; proba-бадж на ветке `badges`
  зелёный для 0.38.0; `UPGRADING.md` и §13 содержат записи этого релиза; тег `v0.38.0`.
- Якоря: `gradle.properties`, `UPGRADING.md`, `kompot-spec/SPEC.md` §13, `kompot-bom/`.

## Находки

### Итерация 1 — 2026-09-25

Все задачи из `blocked_by` закрыты. Подготовлено то, что обратимо:

- **Провод:** `checkSchemaCompatibility` против `v0.37.0` — 21 изменение, все совместимые (новые типы
  в деградирующих иерархиях, поля с умолчанием, `fallback` в базе `KompotComponent`). В §13 писать
  нечего: ни одно слово не шло мимо §15.
- **`UPGRADING.md`:** восемь объявленных ломающих коммитов с `v0.37.0`. Шесть уже были записаны. Два
  нет: смена булева `drawnAsFallback` на `KompotDegradationOutcome` (#147) не имела записи вовсе — она
  вышла раньше, чем появилась проверка `breaking_changes.py`, — а смена `KompotStudioConfig.samples`
  (#154) стояла под заголовком «0.37.0», хотя влита после тега. Запись добавлена, заголовок исправлен.
- **ABI:** дампы появились 19.09 (#156), на теге `v0.37.0` их нет. С их появления удалений, кроме
  объявленных конструкторов data class, нет. Изменения между тегом и дампами машиной не сравнить —
  оттуда найдены только #147 и #154, оба теперь в журнале.
- **README:** словарь в README не перечисляется (он в SPEC), но `kompot-client/README.md` не говорил,
  что действия 0.38 ничего не делают без своего звена в цепочке обработчиков. Добавлен раздел
  «Действия, которые клиент исполняет сам» со сниппетом под проверкой `readme_snippets.py`.

## Вопрос

Остальное необратимо и по этому пункту делается человеком (бандл Central читается глазами до
Publish). Решает владелец kompot и viddik.

1. **viddik.** Исправление youndie/viddik#44 закрыто 24.09, а последний релиз viddik на Central —
   0.6.0 от 20.09, то есть исправления в нём нет. Варианты:
   - выпустить viddik 0.6.1 на Central и перевести kompot на него (цикл готовит PR в kompot после
     выхода релиза);
   - вернуть viddik 0.6.0 и обход #44 (его убрали в #161);
   - выпустить kompot 0.38.0 с test-only snapshot'ом — в опубликованное он не попадает, но сборка по
     тегу будет зависеть от Reposilite.
2. **Публикация kompot 0.38.0** — диспатч `central.yaml` в sborka, чтение бандла, Publish, тег
   `v0.38.0`, проверка proba-баджа. Цикл может запустить диспатч и проверить результат; Publish и тег —
   по слову владельца.

## Ответ — 2026-09-25

Владелец: **выпустить viddik 0.6.1**; публикацию kompot **запускает цикл, Publish нажимает владелец**.

### Итерация 2 — 2026-09-25

- viddik: версия 0.6.1 (youndie/viddik#46), диспатч `central.yaml` в sborka — бандл в staging,
  Publish нажал владелец; 0.6.1 разрешается с Central (четыре артефакта и маркер плагина). Тег
  `v0.6.1` на `3460e3c`, релиз на GitHub.
- kompot переведён со snapshot `0.6.0.40` на `0.6.1`. Проверено заодно: в опубликованное kompot viddik
  не попадает — он в `desktopTest` и в `studioRuntime`, отдельной конфигурации запуска студии; POM
  0.37.0 с Central viddik не содержит. Полный `./gradlew build` на Linux-машине — 1273 теста, 0
  упавших, с первой попытки.
- Дальше: после вливания — диспатч `central.yaml` для kompot `0.38.0` с `main`, чтение бандла и
  Publish — владелец, затем тег `v0.38.0`, проверка `kompot-bom:0.38.0` на Central и proba-баджа.
